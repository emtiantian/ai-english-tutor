# AI English Tutor 优化计划

参考开源项目:

- [moeru-ai/airi](https://github.com/moeru-ai/airi)(Live2D + VRM 双角色、20+ LLM provider、浏览器内推理、MCP Launcher)
- [Open-LLM-VTuber/Open-LLM-VTuber](https://github.com/Open-LLM-VTuber/Open-LLM-VTuber)(Live2D 桌宠、VAD + 语音打断、视觉感知、内心独白、proactive)

---

## 总览

本计划保留 5 项功能：P1 沉浸感（① 自动动作触发、② VAD 打断）、P2 教学增强（⑥ Proactive、⑦ 长期记忆）、P3/P4 部署（⑫ CosyVoice 远程部署稳定性优化）。④ ASR 流式/本地、⑧ 多老师角色卡已移除，统一使用浏览器 ASR。

### 迭代波次与优先级

| 波次            | 项目                            | 借鉴自             | 工作量   | 优先级  |
| --------------- | ------------------------------- | ------------------ | -------- | ------- |
| **P1 沉浸感**   | ① 自动附加 Live2D 表情/动作触发 | Open-LLM-VTuber    | S(1-2d)  | 🟡 Med  |
| **P1 沉浸感**   | ② VAD + 语音打断                | Open-LLM-VTuber    | M(4-5d)  | 🔴 High |
| **P2 教学增强** | ⑥ Proactive 主动开口            | Open-LLM-VTuber    | S(2d)    | 🟡 Med  |
| **P2 教学增强** | ⑦ 长期记忆 / 学习画像           | airi(Memory Alaya) | L(7-10d) | 🟡 Med  |
| **P3/P4 部署**  | ⑫ CosyVoice 远程部署稳定性优化  | —                  | S(2-3d)  | 🟡 Med  |

> S = 1-3d / M = 3-7d / L = 7+d(单人工作量估计)

### 跨功能依赖关系

```
① 自动动作触发 ──┬─→ ② VAD 打断（被打断时也可触发一个“被打断”动作）
                └─→ ⑥ proactive（主动开口也带动作）

⑥ proactive ────→ ⑦ 长期记忆（主动开口靠记忆驱动选话题）

⑫ CosyVoice 部署优化 ──→ 独立,只涉及运维侧
```

### 全局风险

- **音频管线复杂度**：VAD + 打断 + 浏览器 ASR 三者交织在同一条录音/播放链路上，需要分阶段引入，**不要混在一个 PR 里**。
- **ASR 策略收敛**：④ 已砍掉，统一使用浏览器原生 `Web Speech API`。远程服务器上若仍有自托管 ASR 服务，需检查并关闭，避免资源浪费，但保留 `/home/haohe/data` 下的模型文件不删除。
- **prompt 系统膨胀**：⑥/⑦ 都需要改 `ai/prompts/`，先统一 prompt 注入机制再加功能。
- **会话状态膨胀**：`SessionManager` 已经承担会话 + 场景 + 词汇，再加长期记忆可能撑爆，⑦ 上线前考虑拆分。

---

## ① 自动附加 Live2D 表情/动作触发 【P1】

### 目标 / 用户体感

老师每次回复时，Live2D 角色都会同步做一个表情或动作（微笑、点头、鼓励手势等），让教学过程更像真人互动。不再依赖 LLM 在文本里写内心独白或 `*tag*`，而是后端根据回复语义/场景自动挑选动作。

**对比**：Open-LLM-VTuber 把 inline thought 单独渲染驱动表情；我们选择更稳定的方式——后端在每次 SSE `teacher.response` 时额外下发 `motion` / `expression`，前端立即触发。

### 现状评估

- 已有：`packages/shared/src/motion-analyzer.ts` 文本→motion 启发式映射
- 已有：`packages/shared/src/types.ts` 中 `AVAILABLE_MOTIONS` / `AVAILABLE_EXPRESSIONS` 定义语义动作/表情集合；具体模型映射由各 Live2D manifest 的 `motionRegistry` 提供
- 已有：SSE event `teacher.response` 含 `motion` / `expression` 字段
- 缺：当前 `motion` / `expression` 不是**每次回复都发**，触发率不稳定
- 缺：没有“回复语义 → 默认动作”的兜底策略，导致很多回复没有动作

### 实现方案

**关键思路**：每次老师回复都**强制附加**一个动作/表情。优先级：

1. 若 `motion-analyzer` 从回复文本中识别出明确情绪（鼓励/纠正/思考/告别等），用该语义动作；
2. 若无法识别，按会话上下文兜底（例如：用户刚答对 → `happy`/`thumbs-up`；用户答错 → `thinking`/`gentle`；开场/结束 → `wave`/`nod`）；
3. 兜底仍失败时，随机从白名单选一个轻动作（如 `nod`、`smile`），保证绝不空过。

**动作挑选时机**：

- 非流式/流式回复的**第一句**发出时，立即 emit `teacher.motion` SSE event；
- 前端 Live2D Provider 监听 `teacher.motion`，不等整句结束就触发动作；
- tag 文本不显示在 chat UI 里（本方案不生成 tag，无需过滤）。

**白名单约束**：

- 可用动作集合定义在 `packages/shared/src/types.ts` 的 `AVAILABLE_MOTIONS` / `AVAILABLE_EXPRESSIONS` 中；
- 后端挑选后通过 `motionRegistry` 映射到当前 Live2D 模型的实际动作名。

### 任务拆解

- [ ] **T1.1** 在 `motion-analyzer.ts` 增加“默认动作选择”函数：`pickMotion(text, context?)`，返回 `{ motion, expression }`，无法识别时返回兜底动作
- [ ] **T1.2** 在 `engine.ts` 每次生成老师回复的入口调用 `pickMotion`，把结果写入 SSE `teacher.response` 的 `motion` / `expression` 字段
- [ ] **T1.3** 若回复分多句，只取第一句做语义匹配，避免动作过于频繁；后续句子不再重复触发
- [ ] **T1.4** 前端 `App.vue` / Live2D Provider 确保收到 `teacher.response` 中的 `motion` 字段后立即触发动作（不再等回复结束）
- [ ] **T1.5** 兜底策略可配置：在 `.env` / 前端设置里提供 `MOTION_FALLBACK=random|context|none`，默认 `context`
- [ ] **T1.6** 单测覆盖 `pickMotion`：鼓励句、纠错句、无情绪句、空文本、多句只取首句

### 风险与依赖

- 动作太频繁会分散注意力 → 只触发一次，且优先轻动作（nod/smile）
- 不同模型行文风格差异 → 不依赖 LLM 输出 tag，纯本地规则，不受模型影响
- 当前只有默认 Live2D 模型 → 动作映射先基于默认 `motionRegistry`，后续若加多模型再扩展

### 验收标准

- 连续 20 次老师回复中，≥90% 触发至少一个 Live2D 动作/表情
- 动作在回复开始后的首个 token 或首个句子处触发，**不等整句结束**
- chat UI 不显示任何动作标记文本
- `pnpm --filter tutor-app test` 与 `pnpm --filter @ai-english-tutor/server test` 全绿

---

## ② VAD + 语音打断 【P1】

### 目标 / 用户体感

- 用户不需要按住按钮，**说一句停一下，老师自动开始回复**
- 老师正在说话时，用户开口立即把老师的 TTS 静音并打断当前 LLM 请求，**像真人对话**

**对比**：Open-LLM-VTuber 的 `voice interruption without headphones` 是其招牌功能；我们目前应该是 push-to-talk（需确认）。

### 现状评估

- 已有：`apps/tutor-app/src/composables/useAudioRecorder.ts`（录音）、`useAudioPlayback.ts`（播放）
- 已有：`audio/recorder.ts` 和 `audio/player.ts`
- 缺：**没有 VAD**，需要用户主动按按钮录音
- 缺：**没有打断机制**，老师 TTS 在播，LLM stream 在推，用户开口被完全忽略

### 实现方案

**前端 VAD**：用 [`@ricky0123/vad-web`](https://github.com/ricky0123/vad)（Silero VAD 的 WASM 封装，~1MB，浏览器端跑）

- 持续监听麦克风（可由用户开关）
- `onSpeechStart`：暂停 TTS 播放、abort 当前 SSE/LLM 请求、开始累积 audio chunks
- `onSpeechEnd`：把累积的 audio 上传 ASR（短停顿 800-1500ms 触发）

**打断管线**（三层抓手）：

1. **TTS 播放**：`useAudioPlayback.ts` 暴露 `abort()`，清空 AudioContext queue
2. **LLM SSE**：`tutor-client` 持有当前 `AbortController`，VAD `onSpeechStart` 触发 abort
3. **后端**：接收到 abort signal 后，`engine.ts` 把当前会话标记为 `interrupted`，session 历史里保留**已生成的部分回复**（给 LLM 自然续接）

**echo cancellation**（避免老师声音被 VAD 误判）：

- `getUserMedia({ audio: { echoCancellation: true, noiseSuppression: true } })` 浏览器原生 AEC 大部分场景够用
- 边缘场景：在播放 TTS 期间临时降低 VAD 灵敏度，或基于音量阈值二次过滤

### 任务拆解

- [ ] **T2.1** 引入 `@ricky0123/vad-web`，封装 `composables/useVAD.ts`，暴露 `onSpeechStart/End` 事件 + `enable/disable`
- [ ] **T2.2** 设置项：`pages/Settings.vue` 加“免提对话(beta)”开关，默认关
- [ ] **T2.3** `useAudioPlayback.ts` 加 `abort()` 方法，清空 AudioContext queue + 标记中断
- [ ] **T2.4** `tutor-client.ts` 维护 currentAbortController，暴露 `interrupt()` 方法
- [ ] **T2.5** `stores/tutor.ts` 监听 VAD 事件，组合调用 `playback.abort() + client.interrupt() + recorder.start()`
- [ ] **T2.6** 后端 `routes/chat.ts` + `engine.ts`：接收新 endpoint `POST /api/chat/interrupt`，把当前 session 标记为打断状态，保留已生成内容到会话历史
- [ ] **T2.7** SSE `teacher.interrupted` 事件用于前端 UI 反馈（“被打断了…”）
- [ ] **T2.8** 集成测试：模拟“老师说一半，用户开口”场景，断言 TTS 停止、LLM abort、新 ASR 请求开始
- [ ] **T2.9** iOS Safari 适配：VAD 需要在 user gesture 后启动 AudioContext（已有 unlock 机制，复用）

### 风险与依赖

- **AEC 不够好导致老师自己打断自己**：这是 Open-LLM-VTuber 也承认的难题，先用浏览器原生 AEC + 播放时降灵敏度，实在不行加“按住空格强制录音”逃生入口
- **VAD WASM 模型 1MB+**：首次加载延迟，放 lazy import，只在开关打开时下载
- **会话状态机变复杂**：多了 `speaking → interrupted → resuming` 几个状态，要在 `session-manager` 里画清楚
- **ASR 策略**：本功能依赖浏览器 ASR；④ 已砍掉，不使用远程/本地 ASR 服务

### 验收标准

- 安静环境下用户说话延迟 < 300ms 触发录音
- 老师 TTS 播放中，用户开口 < 500ms 内 TTS 静音
- 被打断后，LLM 不再生成，token 不会浪费
- 误触发率 < 5%（在键盘敲字 / 鼠标点击噪音下）

---

## ⑥ Proactive 主动开口 【P2】

### 目标 / 用户体感

- 用户静默 N 秒，老师主动说话：“You seem quiet, want to try another sentence?”
- 用户连续犯同类错误，老师主动指出：“I noticed you struggle with 'th' sound, let's practice it”

**对比**：Open-LLM-VTuber 的 proactive 是无条件触发；我们做**教学场景驱动**会更有价值。

### 现状评估

- 已有：`SessionManager` 维护会话历史
- 已有：`VocabTracker` 跟踪场景词汇进度
- 缺：**前端没有 idle timer**
- 缺：**没有 proactive prompt 模板**

### 实现方案

**触发条件**（由前端检测，后端生成内容）：

1. **静默触发**：用户 30s 无任何输入（语音/打字）→ 调用 `/api/chat/proactive`
2. **进度触发**：场景词汇进度卡住超过 3 轮 → 老师主动给提示
3. **错误模式触发**：`VocabTracker` 记录到同一发音错误 ≥ 2 次 → 主动开口

**实现**：

- 后端新增 `engine.proactive(sessionId, reason)` 方法，根据 reason 选不同 prompt 模板
- 前端 `stores/tutor.ts` 加 idle timer，phase 为 `teaching` 时启动
- LLM 输出走和普通回复完全一样的 SSE 通道，前端无需特殊处理（只是看起来“AI 自己说话了”）

### 任务拆解

- [ ] **T6.1** `ai/prompts/proactive.ts` 新模板，接受 `reason: 'idle' | 'stuck' | 'error-pattern'` 切换风格
- [ ] **T6.2** `engine.ts` 加 `proactive(sessionId, reason, context?)` 方法
- [ ] **T6.3** 新路由 `POST /api/chat/proactive` body `{ sessionId, reason }`，触发后通过 SSE 推回复
- [ ] **T6.4** 前端 `useIdleTimer.ts` composable：监听用户输入，N 秒无活动触发回调
- [ ] **T6.5** `stores/tutor.ts` 注册 idle timer，phase=teaching 时启动，N 默认 30s 可配置
- [ ] **T6.6** `VocabTracker` 加 `getStuckPattern()` API 检测错误重复
- [ ] **T6.7** UI 提示：proactive 消息用稍微不同的样式（如左侧加 “💭” 图标）区分

### 风险与依赖

- 太烦人：proactive 频率必须有节流（同一会话 5 分钟内最多 2 次）
- 用户在思考（不是真的 idle）：区分“思考中”和“走神”，N 默认 30s 而非 10s
- **依赖 ① 自动动作触发**：proactive 消息也走 `pickMotion`，触发“招手 / 戳一戳”动作更生动
- **依赖 ⑦ 长期记忆**：proactive 触发时去查记忆，选最相关话题主动开口（强依赖，⑦ 完成后 ⑥ 效果才完整）

### 验收标准

- idle 30s 触发率 100%，但同会话节流生效
- 用户开始输入后 idle timer 立即重置
- 教学连续性：proactive 内容能引用前面的对话（不是 hello 寒暄）

---

## ⑦ 长期记忆 / 学习画像 【P2】

### 目标 / 用户体感

- 老师记得“上次说你 'th' 发音不准”、“你说过你喜欢旅行场景”、“你的目标是雅思 6.5”
- 跨会话学习画像：词汇掌握度、薄弱发音、偏好场景、对话风格（formal/casual）

**对比**：airi 的 `Memory Alaya` 是 WIP，用 DuckDB WASM + pgvector；我们后端有 SQLite，加 `sqlite-vec` 比纯浏览器方案更可控。

### 现状评估

- 已有：`SessionManager` SQLite 持久化（但只存对话，不存抽象 fact）
- 已有：`VocabTracker` 词汇掌握度（隐式记忆）
- 缺：**没有 fact 抽取**
- 缺：**没有向量检索**

### 实现方案

**数据模型**（SQLite）：

```sql
-- 用户画像（每用户一行）
CREATE TABLE user_profile (
  user_id TEXT PRIMARY KEY,
  cefr_level TEXT,            -- 当前评估等级
  goals TEXT,                 -- 'IELTS 6.5', 'travel chat'
  preferences JSON,           -- 学习偏好
  updated_at INTEGER
);

-- 记忆条目（向量检索）
CREATE TABLE memories (
  id INTEGER PRIMARY KEY,
  user_id TEXT NOT NULL,
  kind TEXT,                  -- 'fact' | 'weakness' | 'preference' | 'achievement'
  content TEXT,               -- 自然语言事实
  embedding BLOB,             -- sqlite-vec
  source_session TEXT,
  importance INTEGER,         -- 0-10，低重要性会被淘汰
  created_at INTEGER,
  last_accessed INTEGER
);

CREATE VIRTUAL TABLE memories_vec USING vec0(embedding float[768]);
```

**写入路径**（每次会话结束）：

1. `engine.ts` 调 `memory-extractor.ts`，用 LLM 把这次会话总结成 3-5 条 fact（prompt 模板约束输出 JSON）
2. 对每条 fact 算 embedding（用 OpenAI text-embedding-3-small 或本地 ONNX）
3. 写入 `memories` 表

**读取路径**（每次会话开始 / 长回合）：

1. 取当前用户最近输入向量化
2. `sqlite-vec` top-K 召回 5 条最相关记忆
3. 注入 system prompt：`Background about this student: ...`

**淘汰策略**：总量上限 200 条/用户，超出按 `importance` + `last_accessed` 时间衰减淘汰。

### 任务拆解

- [ ] **T7.1** 引入 `sqlite-vec` 扩展（better-sqlite3 加载 .so/.dylib），`db/migrations/` 加表 schema
- [ ] **T7.2** `ai/memory/embedder.ts` 抽象 embedding provider（默认 openai，可切本地）
- [ ] **T7.3** `ai/memory/extractor.ts`：会话结束时调 LLM 提取 fact，prompt 严格 JSON 输出
- [ ] **T7.4** `ai/memory/store.ts`：增删改查 + 向量召回 + 淘汰
- [ ] **T7.5** `engine.ts` 集成：会话开始时调 `store.recall()` 注入 prompt，会话结束触发 `extractor.extract()`（异步，不阻塞响应）
- [ ] **T7.6** UI 新增“学习画像”页面：展示已记录的 fact，允许用户编辑/删除（隐私 + 准确性）
- [ ] **T7.7** 数据导出/导入：用户可下载 JSON 备份
- [ ] **T7.8** 默认 user_id 用浏览器持久化 ID（没登录系统时），登录后绑定

### 风险与依赖

- LLM 抽取 fact 出错 → 给用户编辑入口（T7.6）兜底
- 隐私：记忆里可能存敏感信息，**默认本地存储，不上传**，部署模式才入 SQLite
- embedding 成本：每会话调一次 embedding API，可换本地 ONNX（`Xenova/all-MiniLM-L6-v2` 23MB）
- **依赖 ⑥ proactive**：proactive 触发时去查记忆，选最相关话题主动开口

### 验收标准

- 连续 3 个会话后，第 4 个会话开始时老师能引用至少 1 条历史 fact
- 记忆召回准确率（人工评估）≥ 70%
- 单用户 1000 条记忆下，召回延迟 < 50ms
- 用户能在 UI 看到、编辑、删除自己的记忆

---

## ⑫ CosyVoice 远程部署稳定性优化 【P3/P4】

### 目标 / 用户体感

- 远程服务器 (`100.100.132.72`) 上自托管的 `CosyVoice2-0.5B` TTS 服务能随系统自启、崩溃自动恢复、升级不丢音色配置。
- 本地开发机连接远程 TTS 时不受 HTTP 代理干扰，开箱即用。
- 部署流程把 `docker-compose.cosyvoice.yml`、`spk2info.pt` 生成、健康检查都纳入版本管理，而不是每次手动 `docker run`。

### 现状评估

- 已有:`cosyvoice:local` 镜像(22GB)和模型缓存(`/data/modelscope-cache`)。
- 已有:`docker-compose.cosyvoice.yml` 已修复(去重、补 `command` 和 `tutor-net`)。
- 已有:自定义音色 `EnglishTutor` 的 `spk2info.pt` 已生成,挂载在 modelscope 缓存中。
- 缺:**没有 healthcheck**,容器启动后模型还在加载,外部无法判断 Ready。
- 缺:**没有 systemd 自启**,重启服务器后需要手动 `docker compose up`。
- 缺:**没有日志限制/切割**,长期运行可能占满磁盘。
- 缺:**spk2info 生成没有脚本化**,换音色或换机器时要手工写 Python。
- 风险:GTX 970 只有 4GB 显存,模型加载后已用 ~3.8GB,长句/并发会 OOM。

### 实现方案

**1. compose 层面加固**

```yaml
services:
  cosyvoice:
    image: cosyvoice:local
    ports:
      - '50000:50000'
    volumes:
      - ${AI_TUTOR_HOME:-~/.ai-english-tutor}/data/modelscope-cache:/root/.cache/modelscope
      - ${AI_TUTOR_HOME:-~/.ai-english-tutor}/data/cosyvoice-models:/opt/CosyVoice/CosyVoice/pretrained_models
      - ${AI_TUTOR_HOME:-~/.ai-english-tutor}/data/cosyvoice-spk2info:/opt/CosyVoice/spk2info:ro # 新增:音色持久化目录
    command:
      [
        bash,
        -c,
        'cd /workspace/CosyVoice && python runtime/python/fastapi/server.py --port 50000 --model_dir iic/CosyVoice2-0.5B'
      ]
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    networks:
      - tutor-net
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: '50m'
        max-file: '5'
    healthcheck:
      test:
        [
          'CMD-SHELL',
          'python -c "import urllib.request; urllib.request.urlopen(''http://localhost:50000/inference_sft'', data=b''tts_text=test&spk_id=EnglishTutor'', timeout=10)"'
        ]
      interval: 30s
      timeout: 15s
      retries: 5
      start_period: 120s

networks:
  tutor-net:
    external: true
```

**2. 音色(spk2info)脚本化**

新增 `scripts/build-cosyvoice-spk2info.mts`:

- 输入:参考音频路径 + 音色 ID + 模型目录
- 输出:`spk2info.pt`
- 运行方式:在具备 GPU 的机器上通过 `docker run --rm --gpus all cosyvoice:local` 执行,无需本机装 conda。
- 默认内置 1 个 `EnglishTutor` 音色;用户可在 `data/cosyvoice-spk2info/` 放自己的参考音频。

**3. systemd 自启服务**

新增 `scripts/cosyvoice.service`:

```ini
[Unit]
Description=AI English Tutor CosyVoice TTS
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
WorkingDirectory=/home/haohe/data/.ai-english-tutor/app
Environment=AI_TUTOR_HOME=/home/haohe/data/.ai-english-tutor
ExecStart=/usr/bin/docker compose -f docker-compose.yml -f docker-compose.cosyvoice.yml up -d
ExecStop=/usr/bin/docker compose -f docker-compose.yml -f docker-compose.cosyvoice.yml down
TimeoutStartSec=300

[Install]
WantedBy=multi-user.target
```

安装:

```bash
sudo cp scripts/cosyvoice.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable --now cosyvoice
```

**4. 本地开发机代理配置文档化**

在 `README.md` / `.env.example` 注释中说明:若本机有 `http_proxy`,必须将远程 TTS IP 加入 `NO_PROXY`:

```bash
export NO_PROXY="localhost,127.0.0.1,::1,*.local,100.100.132.72"
```

**5. 可选:GPU 升级或切托管**

如果 RTF 仍大于 2,提供两种预案:

- **硬件升级**:换 RTX 3060/4060 以上(≥8GB 显存)。
- **切 SiliconFlow 托管**:只改 `.env` 中的 `TTS_PROVIDER` 和 `SILICONFLOW_API_KEY`,无需再维护 GPU 机器。

### 任务拆解

- [ ] **T12.1** `docker-compose.cosyvoice.yml` 增加 `healthcheck`、`logging` 限制、独立 `spk2info` 挂载目录。
- [ ] **T12.2** 新增 `scripts/build-cosyvoice-spk2info.mts`,支持从参考音频生成 `spk2info.pt`。
- [ ] **T12.3** 把当前 `EnglishTutor` 的参考音频和 `spk2info.pt` 放入 `data/cosyvoice-spk2info/` 并纳入部署同步。
- [ ] **T12.4** 新增 `scripts/cosyvoice.service` 和安装说明,实现开机自启。
- [ ] **T12.5** `scripts/deploy-to-server.sh` 自动重启 `cosyvoice` 服务或 compose。
- [ ] **T12.6** `README.md`/`.env.example` 增加“远程 TTS + NO_PROXY”说明。
- [ ] **T12.7** 后端启动时增加 TTS 健康检查:若 `TTS_PROVIDER=cosyvoice`,启动后调用一次 `/inference_sft` 探测,失败则日志告警。

### 风险与依赖

- healthcheck 首次需要等模型加载(约 90-120s),`start_period` 要足够长。
- 4GB 显存是硬瓶颈,healthcheck 探测本身也会占显存,不要把间隔设太短(建议 ≥30s)。
- 该计划**不阻塞现有功能**,属于部署侧优化,可独立排期。

### 验收标准

- 服务器重启后 2 分钟内 `curl http://localhost:50000/inference_sft` 返回 200。
- `docker logs` 不超过 250MB(5×50m)。
- 换机器部署时,执行 `pnpm push:server` + `build-cosyvoice-spk2info` 即可恢复音色,无需手工 Python。
- 本地 `pnpm local` 在只设置 `.env` 的情况下可直接连远程 TTS,无需每次手动 export `NO_PROXY`。

---

## 附录:不在本次计划内的项目

| 编号                   | 项目                                                                | 不做的理由                            |
| ---------------------- | ------------------------------------------------------------------- | ------------------------------------- |
| ③ 桌宠模式             | 需要 Tauri 包装,工作量大,留待 P5                                    |
| ④ ASR 流式 + 本地选项  | 远程/本地 ASR 服务方案                                              | 统一用浏览器 ASR，砍掉服务端 ASR 方案 |
| ⑤ 视觉输入(截屏给老师) | 价值大但当前 LLM provider 不全支持,后期接 GPT-4o/Claude vision 再做 |
| ⑧ 多老师角色卡         | 多 persona + 多音色                                                 | 当前不需要,保留默认老师               |
| ⑨ MCP 集成             | 当前教学场景没有非常明确的 tool 需求,先观察                         |
| ⑩ VRM(3D 角色)         | 美术差异化项,Live2D 已够用,优先级低                                 |
| ⑪ LLM Provider 统一    | xsai 风格统一 provider                                              | 已完成,从计划中移除                   |

---

## 执行进度

| 项目                            | 阶段  | 状态      |
| ------------------------------- | ----- | --------- |
| ⑫ CosyVoice 远程部署稳定性优化  | P3/P4 | 🚧 进行中 |
| ① 自动附加 Live2D 表情/动作触发 | P1    | 待办      |
| ② VAD + 语音打断                | P1    | 待办      |
| ⑥ Proactive 主动开口            | P2    | 待办      |
| ⑦ 长期记忆 / 学习画像           | P2    | 待办      |

---

_文档创建时间:2026-06-25_
_参考调研结论:对话上文_
_最后更新:2026-07-21 按最新决策恢复 ②⑥⑦；保留改写后的 ① 和进行中的 ⑫；移除 ④⑧⑪。_
